export interface DashboardSummary {
    totalValue: number; // Presupuesto total
    totalExecuted: number; // Ejecuciones de Proyectos
    averagePhysicalProgress: number; // Avance Físico Promedio
    projectCount: number;
}